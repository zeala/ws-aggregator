//var scrape = require('scrape')
Meteor.methods({

    getWebsiteData: function (url){

        //this.unblock();
        //console.log("getWebsiteData, url : " + url);
        //return Meteor.http.call("GET", url);
        //var websiteData = Scrape.website(url);
        /*var websiteData = Scrape.website(url);
        console.log(websiteData);
        return websiteData;*/
    }
});

